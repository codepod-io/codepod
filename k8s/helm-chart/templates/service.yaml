apiVersion: v1
kind: Service
metadata:
  name: {{ include "codepod.fullname" . }}
  labels:
    {{- include "codepod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "codepod.selectorLabels" . | nindent 4 }}
