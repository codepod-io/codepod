apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "codepod.fullname" . }}-test-connection"
  labels:
    {{- include "codepod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "codepod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
